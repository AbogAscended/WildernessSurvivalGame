import random

class Items:
    itemType, isRepeating, itemAmount

    def __init__(type, repeat, amount):
        itemType = type
        isRepeating = repeat,
        itemAmount = amount 

    def take():
        pass